---
interface Props {
    class?: string;
    href: string;
}

const { class: clazz = "", href } = Astro.props;
---

<a href={href} class:list={["underline text-blue-500 hover:text-blue-600 dark:text-blue-300 dark:hover:text-blue-200", clazz]}>
    <slot />
</a>
