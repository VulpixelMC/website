---
interface Props {
	class?: string;
	url: string;
}

const { class: clazz = "", url } = Astro.props;
---

<a href={url} class:list={["underline text-blue-500 hover:text-blue-600 dark:text-blue-300 dark:hover:text-blue-200", clazz]}>
	<slot />
</a>
